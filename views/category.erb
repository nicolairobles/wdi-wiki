<div class="page-header">
	<!-- Category Header -->
  <h1><%= @title %></h1>
</div>

<!-- Categories Wrapper 100% width -->
<div class="categories-wrapper">
	<% @articles.each do |article| %>

	<div>
		<a href=<%="/articles/#{article["article_id"]}"%> ><%= article["article_title"] %>
		</a>
	</div>

	<% end %>
</div>
